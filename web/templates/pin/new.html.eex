<h2>New pin</h2>

<%= render "form.html",
           changeset: @changeset,
           action: user_bucket_pin_path(@conn, :create, @bucket, @current_user) %>

<%= link "Back", to: user_bucket_pin_path(@conn, :index, @bucket, @current_user) %>
