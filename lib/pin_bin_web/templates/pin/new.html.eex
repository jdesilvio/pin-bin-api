<h2>New pin</h2>

<%= render "form.html",
           changeset: @changeset,
           action: user_bin_pin_path(@conn, :create, @current_user, @bin) %>

<%= link "Back", to: user_bin_pin_path(@conn, :index, @current_user, @bin) %>
