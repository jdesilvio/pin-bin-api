<h2>Edit pin</h2>

<%= render "form.html",
           changeset: @changeset,
           action: user_bin_pin_path(@conn, :update, @current_user, @pin.bin_id, @pin) %>

<%= link "Back", to: user_bin_pin_path(@conn, :show, @current_user, @pin.bin_id, @pin) %>
